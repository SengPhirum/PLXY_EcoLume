# Development broker configuration. Production must use TLS 1.2+, per-device
# credentials/ACLs, backend-only subscriptions, and certificate rotation.
persistence true
persistence_location /mosquitto/data/
log_dest stdout
listener 1883 0.0.0.0
allow_anonymous true

